
import SwiftUI

struct BudgetView: View {
    var body: some View {
        NavigationView {
            VStack {
                Text("카테고리별 예산 설정 및 지출 기록")
                    .font(.largeTitle)
                    .padding()
                // TODO: 여기에 카테고리별 예산 설정 및 지출 기록 UI를 추가합니다.
                Spacer()
            }
            .navigationTitle("예산")
        }
    }
}
